# Local Runtime

L-02 temporary local runtime composition and application-boundary package.

This package composes the accepted in-memory TypeScript services for the local operator console. It is not persistence, does not own business records, and is designed to be replaced by SQLite-backed adapters in L-03 without changing UI or accepted service contracts.
