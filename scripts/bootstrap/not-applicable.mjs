const [, , commandName, replacementStep] = process.argv;

console.log(
  `${commandName} is not yet applicable in BE-01. It must be replaced when ${replacementStep} introduces this validation layer.`
);
